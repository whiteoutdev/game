export const ModifierType = {
    PERK     : 'perk',      // Permanent modifiers to base stats (e.g. actor perks)
    PASSIVE  : 'passive',   // Long term modifiers to stats (e.g. long-lived passive buffs/debuffs)
    EQUIPMENT: 'equipment', // Stat modifiers provided by equipment
    TEMPORARY: 'temporary'  // Short-lived stat modifiers (e.g. battle buffs/debuffs)
};
